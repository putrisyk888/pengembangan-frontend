import React from "react";

const Bayar = (prop) => {
    return (
        <div>
            <img src={props.gambar} alt="Metode Pembayaran" />
        </div>
    );
};

export default Bayar;